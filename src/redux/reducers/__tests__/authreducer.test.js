import * as reducer from '/Users/naome/Desktop/ah-frontend-poseidon/src/redux/reducers/authReducer.js'
import { request } from "http";

describe("auth reducer",() =>{
    it("it has a default state", () =>{
        expect(request(undefined, {type:'unxpected'})).toEqual({
            state = initialState,

    })
})
})

